class Item:
       
       def __init__(self, name, type_, description, prop):
              self.name = name
              self.type = type_
              self.description = description 
              self.prop = prop 